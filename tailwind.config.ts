// Tailwind CSS v4 uses CSS-based configuration via @theme directive
// See src/app/globals.css for theme configuration
// This file is kept for compatibility but is no longer used for configuration

import type { Config } from "tailwindcss";

const config: Config = {
  // Content paths are automatically detected in v4
  // Dark mode is handled via CSS in v4
};

export default config;
